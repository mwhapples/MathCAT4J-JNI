[package]
name = "mathcat4j"
version = "0.1.0"
edition = "2021"
authors = ["Michael Whapples"]

[lib]
crate-type = ["cdylib"]

[dependencies]
jni = "0.22.4"
mathcat = { path = "../../../../../MathCAT" }

[build-dependencies]
zip = { version = "8.6.0", default-features = false, features = ["bzip2"] }
